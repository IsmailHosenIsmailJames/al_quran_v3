class NavigationInfoModel {
  String? previousStartKey;
  String? previousEndKey;
  String? nextStartKey;
  String? nextEndKey;

  NavigationInfoModel({
    this.previousStartKey,
    this.previousEndKey,
    this.nextStartKey,
    this.nextEndKey,
  });
}
